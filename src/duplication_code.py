

def terrible_duplication(i, j):
    if i > 100:
        if i % 3 < 2:
            for j in range(i):
                x = int(str(j) + str(i))
                if x % 2:
                    break
            else:
                x = 0
        else:
            for k in range(x):
                x += k
    return x


def terrible_duplication2(i, j):
    if i > 100:
        if i % 3 < 2:
            for j in range(i):
                x = int(str(j) + str(i))
                if x % 2:
                    break
            else:
                x = 0
        else:
            for k in range(x):
                x += k
    return x

def terrible_duplication3(i, j):
    if i > 100:
        if i % 3 < 2:
            for j in range(i):
                x = int(str(j) + str(i))
                if x % 2:
                    break
            else:
                x = 0
        else:
            for k in range(x):
                x += k
    return x


def terrible_duplication4(i, j):
    if i > 100:
        if i % 3 < 2:
            for j in range(i):
                x = int(str(j) + str(i))
                if x % 2:
                    break
            else:
                x = 0
        else:
            for k in range(x):
                x += k
    return x


def terrible_duplication5(i, j):
    if i > 100:
        if i % 3 < 2:
            for j in range(i):
                x = int(str(j) + str(i))
                if x % 2:
                    break
            else:
                x = 0
        else:
            for k in range(x):
                x += k
    return x

def terrible_duplication6(i, j):
    if i > 100:
        if i % 3 < 2:
            for j in range(i):
                x = int(str(j) + str(i))
                if x % 2:
                    break
            else:
                x = 0
        else:
            for k in range(x):
                x += k
    return x

def terrible_duplication7(i, j):
    if i > 100:
        if i % 3 < 2:
            for j in range(i):
                x = int(str(j) + str(i))
                if x % 2:
                    break
            else:
                x = 0
        else:
            for k in range(x):
                x += k
    return x

def terrible_duplication8(i, j):
    if i > 100:
        if i % 3 < 2:
            for j in range(i):
                x = int(str(j) + str(i))
                if x % 2:
                    break
            else:
                x = 0
        else:
            for k in range(x):
                x += k
    return x

def terrible_duplication9(i, j):
    if i > 100:
        if i % 3 < 2:
            for j in range(i):
                x = int(str(j) + str(i))
                if x % 2:
                    break
            else:
                x = 0
        else:
            for k in range(x):
                x += k
    return x

def terrible_duplication10(i, j):
    if i > 100:
        if i % 3 < 2:
            for j in range(i):
                x = int(str(j) + str(i))
                if x % 2:
                    break
            else:
                x = 0
        else:
            for k in range(x):
                x += k
    return x

def terrible_duplication11(i, j):
    if i > 100:
        if i % 3 < 2:
            for j in range(i):
                x = int(str(j) + str(i))
                if x % 2:
                    break
            else:
                x = 0
        else:
            for k in range(x):
                x += k
    return x

def terrible_duplication12(i, j):
    if i > 100:
        if i % 3 < 2:
            for j in range(i):
                x = int(str(j) + str(i))
                if x % 2:
                    break
            else:
                x = 0
        else:
            for k in range(x):
                x += k
    return x

def terrible_duplication13(i, j):
    if i > 100:
        if i % 3 < 2:
            for j in range(i):
                x = int(str(j) + str(i))
                if x % 2:
                    break
            else:
                x = 0
        else:
            for k in range(x):
                x += k
    return x